# =============================================================================
# Source-only Properties  —  run on the producer / ingestion server
# kafka-ingest-pipeline — PipelineMain (sources only)
#
# Usage:
#   java -cp target/kafka-ingest-pipeline-1.0.0-fat.jar \
#     io.github.adityassharma.kafka.pipeline.PipelineMain \
#     config/source.properties
#
# Pair with config/sink.properties running on the consumer / indexing server,
# both pointing at the same Kafka bootstrap.servers.
# For a single-server / single-JVM deployment use config/pipeline.properties.
# =============================================================================

# ---------------------------------------------------------------------------
# Kafka Broker Connection
# ---------------------------------------------------------------------------
bootstrap.servers=localhost:9092

# ---------------------------------------------------------------------------
# Message Format  (must match sink.properties on the consumer side)
#   json (default) | avro
# ---------------------------------------------------------------------------
message.format=json

# Avro option 1: file-based schema
#message.schema.file=config/iss-position.avsc

# Avro option 2: Confluent Schema Registry
#message.schema.registry.url=http://localhost:8081

# ---------------------------------------------------------------------------
# Producer tuning
# ---------------------------------------------------------------------------
acks=all
enable.idempotence=true
retries=3
compression.type=snappy

# ---------------------------------------------------------------------------
# Sources
# ---------------------------------------------------------------------------
sources=iss-position,fluentbit

# ---- Source: iss-position --------------------------------------------------
source.iss-position.type=iss-api
source.iss-position.topic=open-notify-iss
source.iss-position.url=http://api.open-notify.org/iss-now.json
source.iss-position.polling.interval.ms=5000

# ---- Source: fluentbit -----------------------------------------------------
source.fluentbit.type=http-listener
source.fluentbit.topic=app-logs
source.fluentbit.port=8080
source.fluentbit.path=/ingest
source.fluentbit.threads=4

# ---------------------------------------------------------------------------
# Security / TLS / SASL  (uncomment exactly one block — must match the broker)
# ---------------------------------------------------------------------------

# --- Mode 1: PLAINTEXT (default) -------------------------------------------
# No additional properties required.

# --- Mode 2: SSL ------------------------------------------------------------
#security.protocol=SSL
#ssl.truststore.location=/path/to/client.truststore.jks
#ssl.truststore.password=changeit
#ssl.keystore.location=/path/to/client.keystore.jks   # mTLS only
#ssl.keystore.password=changeit
#ssl.key.password=changeit
#ssl.endpoint.identification.algorithm=https

# --- Mode 3: SASL_PLAINTEXT -------------------------------------------------
#security.protocol=SASL_PLAINTEXT
#sasl.mechanism=PLAIN
#sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required \
#  username="aditya" \
#  password="aditya-secret";

# --- Mode 4: SASL_SSL -------------------------------------------------------
#security.protocol=SASL_SSL
#ssl.truststore.location=/path/to/client.truststore.jks
#ssl.truststore.password=changeit
#ssl.endpoint.identification.algorithm=https
#sasl.mechanism=PLAIN
#sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required \
#  username="aditya" \
#  password="aditya-secret";

# ---------------------------------------------------------------------------
# Logging / JVM
# ---------------------------------------------------------------------------
app.log.dir=logs
jvm.heap.xms=256m
jvm.heap.xmx=256m
jvm.gc.type=ZGenerational
jvm.gc.opts=-XX:+UseZGC -XX:+ZGenerational -XX:SoftMaxHeapSize=200m -XX:ZCollectionInterval=120 -XX:ConcGCThreads=2
jvm.gc.log.dir=logs/gc
